version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm install

  build:
    commands:
      - echo "Running build step..."
      # Uncomment if your project has a build script
      # - npm run build

  post_build:
    commands:
      - echo "Build finished successfully"

artifacts:
  files:
    - '**/*'
  discard-paths: no
